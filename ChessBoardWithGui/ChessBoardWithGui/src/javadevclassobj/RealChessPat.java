
package javadevclassobj;

import java.util.ArrayList;

public class RealChessPat 
{
    public static void main(String[] args) 
    {
       
       //Class here for testing purposes
       //MyDBManager db = new MyDBManager();
        
        
       // Code used to view Game stats data
       /*
       ArrayList<GameStatsData> GStats = db.getAllGameStatsData();
       
        for(int i = 0; i < GStats.size(); i++)
        {
            GameStatsData s = GStats.get(i);
            System.out.println(s.GameStatsDataToString());
        }
        
        
       
       
       //code used to view Player Data
       
        ArrayList<PlayerData> PData = db.getAllPlayerData();
        
        for(int i = 0; i < PData.size(); i++)
        {
            PlayerData p = PData.get(i);
            System.out.println(p.PlayerDataToString());
        }
        
       
       
       //Code used to view Game Data
        ArrayList<GameData> GData = db.getAllGameData();
        for(int i = 0; i < GData.size(); i++)
        {
            GameData g = GData.get(i);
            System.out.println(g.GameDataToString());
        }
        */
       
    }

}
